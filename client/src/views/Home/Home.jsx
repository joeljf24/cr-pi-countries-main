import Cards from "../../components/Cards/Cards";

const Home = () => {
    // const onSearch = async (name) => {
    //     try {
    //        const { data } = await axios(`http://localhost:3001/countries/${name}`)
           
    //        if (data.name) {
    //           setCharacters((oldChars) => [...oldChars, data]);
    //        }
  
    //     } catch (error) {
    //        alert('There are no countries with that name!');
    //     }
    // };

    return (
        <div>
            <h3>Hola, esta es la ruta Home</h3>
        </div>
    )
};


export default Home;
